import { Meta, Primary, Canvas, Controls } from 'storybook/blocks'

import * as Story from "./TextBlock.stories.tsx"

<Meta of={Story} />

# Bloco de texto

Este é um componente de texto que pode ser usado para exibir textos com título em uma página.

### Padrão
<Canvas of={Story.Default} />

### Elevado
<Canvas of={Story.Elevated} />

### Gradiente
<Canvas of={Story.Gradient} />

## Documentação Interativa:
<Primary of={Story} />
<Controls />
